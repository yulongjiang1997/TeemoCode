# 团队功能 v2 实施计划

- 配套 spec: `docs/superpowers/specs/2026-08-30-team-v2-design.md`

## Global Constraints

- 纯 UI 侧改动（不改 Rust 壳/引擎）；i18n zh/en 双份；不新增依赖。
- 旧格式消息（`[团队协调]` 前缀）strip 兼容。

## Task 1: teamPreamble v2

- [ ] 生成器重写（标签格式/成员/工作流/协作规则/上限 8）
- [ ] `stripTeamPreamble` 迁入并导出（新 `[mc-team]` + 旧 `[团队协调]` 兼容）
- [ ] `teamPreamble.test.ts`

## Task 2: prefs

- [ ] `TeamWorkflowStep` + `readTeamWorkflow/writeTeamWorkflow`（悬空 roleId 过滤）
- [ ] 测试 round-trip

## Task 3: useComposer 队列修复

- [ ] 入队存 `finalText`（含前缀）；直发路径不变

## Task 4: 内置预设 + TeamSection 增强

- [ ] 4 套内置预设常量（presets）
- [ ] TeamSection: 工作流编辑、预设应用（confirm）、导入导出（剪贴板/粘贴）、上限提示
- [ ] i18n zh/en

## Task 5: LogList

- [ ] 引用 teamPreamble 的 strip（删本地实现）

## Task 6: 验收

- [ ] vitest 全套 + tsc + build；重建 debug exe
